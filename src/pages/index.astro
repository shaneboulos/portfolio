<head>
	<title>Shane Boulos – Portfolio</title>
	<meta
		name="description"
		content="Portfolio and learning journal for Shane Boulos."
	/>
	<meta name="viewport" content="width=device-width, initial-scale=1" />
</head>

<body>
	<main class="page">
		<section class="hero">
			<p class="hero-tagline">PORTFOLIO</p>
			<h1>Hello, I'm Shane Boulos</h1>
			<p class="hero-lead">
				This is the very first draft of my new portfolio.
			</p>
		</section>
	</main>
</body>
<style>
	:root {
		/* Colors */
		--page-bg: #050816;
		--text-main: #f5f5f5;
		--text-muted: #a1a1aa;
		--text-subtle: #d4d4d8;

		/* Font family */
		--font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
			sans-serif;

		/* Type scale */
		--step-2: clamp(2.5rem, 4vw, 3.25rem); /* hero heading */
		--step-1: 1.25rem; /* future subheads */
		--step-0: 1rem; /* body */
		--step--1: 0.875rem; /* small text */
		/* Spacing scale */
		--space-xs: 0.5rem;
		--space-sm: 0.75rem;
		--space-md: 1rem;
		--space-lg: 2rem;
		--space-xl: 3rem;
	}

	body {
		margin: 0;
		font-family: var(--font-sans);
		background: var(--page-bg);
		color: var(--text-main);
	}

	.page {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: var(--space-lg);
	}

	.hero {
		max-width: 640px;
	}

	.hero-tagline {
		text-transform: uppercase;
		letter-spacing: 0.12em;
		font-size: var(--step--1);
		color: var(--text-muted);
		margin-bottom: var(--space-sm);
	}

	.hero h1 {
		font-size: var(--step-2);
		line-height: 1.1;
		margin: 0 0 var(--space-sm_;);
	}

	.hero-lead {
		font-size: var(--step-0);
		line-height: 1.6;
		color: var(--text-subtle);
		margin: 0;
	}

	/* Mobile tweaks */
	@media (max-width: 768px) {
		.page {
			align-items: flex-start;
			justify-content: flex-start;
			padding: 2.5rem 1.5rem;
		}

		.hero {
			max-width: 480px;
			margin: 0 auto;
		}

		.hero h1 {
			font-size: clamp(2rem, 8vw, 2.5rem);
		}

		.hero-lead {
			font-size: 0.95rem;
		}
	}
</style>
